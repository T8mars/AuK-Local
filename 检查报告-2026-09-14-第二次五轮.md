# AuK Local 第二次五轮复查

日期：2026-09-14。对象仍为唯一整合包 `E:\auk\AuK-Local`。本次为上一份五轮报告后的新增检查，采用失败复现、修复回归、子 Agent 独立检查、真实页面生成及关闭演练。

## 五轮检查结果

| 轮次 | 检查范围 | 新发现与处理 |
|---|---|---|
| 1 | 请求身份、源音频一致性 | 同一 UUID 的大写、无横线、花括号写法可建立不同任务；Windows 大小写路径可能共用结果目录。统一 UUID 为标准格式，验证重复提交只保留一项。重试和推理此前未核对源文件，截断或同尺寸修改都能继续执行；现在根据原始帧数和 SHA-256 验证，损坏时要求重新上传，并在加载模型前拒绝。 |
| 2 | ComfyUI 工作流、节点、安装 | 三份工作流的连接参数顺序错误，且缺少 Seed/refresh 的生成后控制值，导入后出现空模型、NFE=-1 等错位。修正 JSON，利用已安装前端原函数和真实 INPUT_TYPES 验证。修复省略可选 token_file 的 TypeError、纯文字生成继续携带旧音频、安装路径中 [] 被当作通配符。 |
| 3 | 数据库与模型替换恢复 | SQLite 的 with connection 只管理事务，未显式关闭连接；改为 finally close，持有连接引用的测试证实已关闭。模型目录替换失败时原目录没有恢复；现在恢复 .previous，且再次启动下载时可恢复中断的目录交换。测试使用临时小文件，不移动真实模型。 |
| 4 | 页面、历史结果、Seed | 缺失 WAV/参数文件可使结果页面报错，坏 JSON 未提示；现保留可用音频并明确提示缺失项，API 对坏参数返回 410。补上历史结果查看入口及页面加载时刷新历史。Seed 改为文本输入，避免浏览器大整数舍入；真实页面生成验证 9007199254740993 保持精确。 |
| 5 | 交叉复核、真实生成、退出 | 子 Agent 复核主 Agent 修改；补齐 LocalClient/TaskManager 等待时对调度暂停的及时报错。真实浏览器生成、历史回看通过。关闭演练发现 Gradio 长连接使 Uvicorn 无限等待，启动配置加入 5 秒连接收尾上限；保持浏览器连接时，测试服务含 worker 清理约 8.91 秒退出。 |

## 测试证据

- 完整 pytest：**74 passed，6 subtests passed**，约 30.40 秒；2 条上游弃用提示，不是失败。相较上一轮 52 项，新增 22 项回归测试。
- 测试用 AuK 自带 Python，测试工具安装于 `audit/test-deps`，未修改包内运行依赖；Comfy 测试加载本机已安装 ComfyUI API，子 Agent 也在实际 Comfy Python 独立验证。
- 包内 pip check：无依赖冲突；Python 编译检查与 Ruff F/E9 错误检查通过；git diff --check 通过。
- 节点及三份工作流已安装至 `E:\comfyui-t8-onekey-5x\ComfyUI\custom_nodes\ComfyUI-AuK-Local`，逐文件比对与源码一致。已打开的 ComfyUI 需重启并重新加载修正后的示例工作流。
- 现有正式数据库未发现大小写任务 ID 冲突。
- 本次没有重新执行全部模型 SHA-256；完整哈希结果见前一份报告，本次模型文件未修改。

### 真实页面生成

- 在临时测试服务 7861 上使用同一份源码、运行环境和模型，隔离测试数据库和输出；没有制作第二个整合包。
- 任务：`067acd5c-bf60-445d-95b3-12167083e329`，AuK-Flash，CPU Offload 开启，任务后不驻留。
- Seed：`9007199254740993`，页面、数据库、参数文件一致。
- 输出：24000 帧 / 24000 Hz / 1 秒；样本全部有限，峰值 0.37305969。
- 记录耗时：37.034 秒；峰值已分配显存：17523233792 字节。
- 文件：`audit/second-five-rounds/outputs/067acd5c-bf60-445d-95b3-12167083e329/result.wav`，同目录保存 metadata.json。
- 浏览器实测：生成完成、播放/下载控件出现、刷新后历史可显示、按任务 ID 回看成功；关闭演练前页面错误日志为空。
- 两次临时测试服务都已退出；最终保留正式 7860 页面。

## 生效状态与未完成项

**修复已写入唯一整合包，节点和示例已同步，但本次没有重启正式 7860 进程。** 自动审批拒绝了停止并重启正式服务的操作，没有提供具体原因。新版在隔离测试服务验证；正式页面仍需重启 AuK 后加载本次修复，ComfyUI 也需重启加载新版节点。不要把源文件修复误认为旧进程已自动更新。

仍需完成：

1. 16 类任务的固定素材主观听测；本次真实短音频生成不代表编辑、分离等全部效果验收。
2. 另一台干净 Windows 机器的非管理员、离线、无系统 Python/Node/Git、完整包迁移验收。本次中文/空格/方括号路径测试只覆盖节点安装与配置迁移。
3. 不同分辨率、125%/150% 系统缩放、录音权限和完整键盘操作覆盖。
4. 真实磁盘写满和整包更新回滚；已有存储故障注入及模型目录替换恢复测试，未真的写满用户磁盘，也未实测整个运行环境的版本回滚。

本次已复现缺陷均已修复并完成对应验证；上述生效步骤和验收边界仍然保留，不能据此承诺所有场景零 BUG。
