# AuK Local 本地整合包

这是一个独立的本地整合包，包含 AuK-Flash、AuK Base、Qwen2.5-Omni-3B、独立 Python/CUDA 12.8 运行环境、FFmpeg/FFprobe 和浅色中文工作台。启动、常见音频解码和推理不依赖系统 Python、Node、Git、FFmpeg 或联网服务。

## 直接使用

双击根目录的 `AuK-Local.exe`。启动器会使用自身所在目录中的 Python、模型和 FFmpeg，服务就绪后自动打开 `http://127.0.0.1:7860`。请保持启动窗口开启；服务停止或发生错误时，窗口会显示退出状态并等待按键，不会自动关闭。

页面顶部提供“检查更新”和“立即更新并重启”。在线更新只安装经过 RSA 签名且逐文件 SHA-256 校验通过的程序包，安装前会备份旧程序，失败时自动回滚；`models`、`runtime`、`ckpts`、`data`、`outputs` 和 `logs` 不会被覆盖。请等待任务结束后再更新，并通过 `AuK-Local.exe` 启动以使用自动重启。

`启动AuK.cmd` 是兼容入口，行为与 EXE 相同，也会在结束后保留窗口，便于查看日志。不要只复制 EXE，请把整个 `AuK-Local` 文件夹完整解压后使用。

首次生成通常需要约 30–110 秒加载模型；默认选择 AuK-Flash。RTX 5090 Laptop 24GB 上建议保持 `CPU Offload` 开启，任务完成后默认释放模型显存。

界面提供 16 类任务。选择需要音频的任务后，会自动显示上传或录音区域。输入音频时长与目标时长之和不能超过 30 秒。每次成功结果保存在 `outputs\任务ID`，包括 32-bit float WAV 和完整 JSON 参数。

## 独立 ComfyUI 节点

ComfyUI 节点是单独的原生插件，不包含在本地整合包中，也不连接本整合包的 7860 服务或令牌。请在 ComfyUI Manager 搜索 **AuK · T8star-Aix**，或从下面的独立仓库安装：

https://github.com/T8mars/Comfyui-Auk-T8

节点在 ComfyUI 进程内直接加载 `ComfyUI/models/auk` 中的模型。使用节点时无需启动本整合包；使用本整合包时也无需安装 ComfyUI。

## 模型与参数

- AuK-Flash：固定 NFE 4、CFG 0，适合作为默认模型。
- AuK Base：默认 NFE 32、CFG 2，可在本地工作台中切换。
- Seed 固定时，请保持模型、输入音频、任务文字与参数一致。
- 历史面板保存任务 ID、模型、状态和错误；输入任务 ID 后可查看历史音频和参数，失败、取消或服务重启中断的任务可按原参数重试。若原始音频被修改或损坏，需重新上传。

## 诊断与修复

双击 `环境诊断.cmd` 执行快速环境、CUDA 和文件尺寸检查。在命令行运行下面的命令可对全部约 24 GiB 模型逐文件执行 SHA-256 校验：

```bat
环境诊断.cmd --verify-hashes
```

如发现缺失或损坏，可在整合包根目录运行：

```bat
set PYTHONPATH=%CD%\src
runtime\python.exe -m auk_local.cli --root "%CD%" download-models --verify-hashes
```

下载过程使用固定 revision，并保留未完成的暂存目录供续传。

模型目录替换失败时会尝试恢复原目录；若替换过程被中断，再次运行下载命令会先恢复保留的 `.previous` 目录。完整整包更新回滚仍需另行验收。

如果界面提示调度暂停，请先恢复磁盘空间或文件访问，再关闭服务窗口并重新启动。已成功结果仍可使用，中断任务可从历史记录重试。完整检查范围和待验收项目见 `检查报告-2026-09-14.md`。

第二次五轮复查见 `检查报告-2026-09-14-第二次五轮.md`。该报告中的旧 ComfyUI HTTP 桥接方案已停止交付，当前节点以独立仓库 2.0.0 及以后版本为准。

第三次五轮复查见 `检查报告-2026-09-14-第三次五轮.md`。查看历史或重试后，页面跟随最近成功选择的任务；旧任务仍在后台排队或运行，可从历史重新选中查看与取消。“取消正在查看的任务”仅作用于当前选中的任务。提交失败会保留已有结果。

## 已验证环境

- Windows 10/11 x64
- NVIDIA GeForce RTX 5090 Laptop GPU 24GB，驱动 576.28
- Python 3.10.20（包内）
- PyTorch / Torchaudio 2.7.1+cu128，BF16
- 24 kHz float WAV 输出

服务只监听 `127.0.0.1`，默认端口为 7860。重复启动时会直接打开或复用现有 AuK 服务。
