# AuK Local 第三次五轮复查

日期：2026-09-14。基于第二次五轮修复提交 `931a54b`，继续检查唯一整合包。正式 7860 服务保持运行；本次使用临时文件、故障注入和隔离页面验证，没有制作第二个整合包。

## 五轮范围与发现

| 轮次 | 范围 | 结果 |
|---|---|---|
| 1 | 音频协议与输入暂存 | 复现并修复采样率/声道数/帧数的小数被静默取整、布尔值被当作整数；编码前也校验采样率。Base64 解码前先核对与帧数相符的编码长度。模拟输入文件写到一半磁盘满，修复失败后残留 input.tmp。 |
| 2 | 页面状态、取消目标、模板 | 子 Agent 复现生成 A 时查看 B，旧 A 轮询抢回显示和取消目标；增加每会话显示操作所有权，过期轮询不再更新页面，后台任务继续保留在历史。无效操作保留已有结果和任务 ID。清空任务/模型及非法预算改为明确提示。克隆参考文字此前未进入模型指令，现保留原字段含义并明确附入参考转录。 |
| 3 | 下载续传与模型完整性 | Hugging Face 会复用同 revision 的本地下载缓存；损坏暂存文件可导致每次续传都校验失败。现在只对查出的坏文件 force_download，并重新校验，健康文件保留。用大小变化及同尺寸坏内容验证，未下载/替换真实模型。另对本机三套模型重新完整 SHA-256 校验，全部 ready，CUDA/BF16 正常。 |
| 4 | 启动、特殊路径和实际页面 | 修复浏览器启动助手的 Resolve-Path 通配符解释。进一步实测 Windows PowerShell 在含 [] 的工作目录启动会把自身位置退回系统目录，造成相对令牌检测失败；两个 CMD 的检查改用进程实际工作目录构造绝对令牌路径。正常/降级/外来服务/无 UI/协议不符及中文空格方括号路径测试通过。 |
| 5 | 交叉复核与回归 | 主 Agent 审查页面改动，子 Agent 独立复查音频、下载、暂存清理和启动修改。覆盖慢提交与后续查看的先后顺序、失败操作保留原显示，以及实际 Gradio 会话间隔离。最终测试和页面证据见下。 |

第 5 轮真实浏览器测试还抓到框架层重放：后台 current_request 已是历史 B，旧 A 生成器结束时 Gradio 的 `handle_streaming_diffs(final=True)` 仍会发送 A 的缓存输出，清掉 B 音频。修复为过期事件先发送 7 个 gr.skip 再结束，确保最终包也保留当前显示；真实 process_api 的两种响应模式及浏览器复测均通过。

## 验证记录

- 音频、下载、输入磁盘满、启动路径问题均先运行失败回归，再验证修复。
- 子 Agent 的 16 模板测试仅核对两个用户文字字段未被丢弃，不代表 16 类真实模型效果通过。
- 下载损坏修复测试使用临时小文件和本地模拟下载器，并核对包内 huggingface_hub 的缓存处理源码；没有重下载真实大模型。
- 完整模型诊断保存于 `audit/diagnostic-third-five-rounds.json`：Flash/Base/Qwen 全部 ready、model_hashes_verified=true、CUDA/BF16=true。
- pip check 无依赖冲突；Ruff F/E9 错误检查通过。
- 最终全套：**118 passed，6 subtests passed**，46.92 秒；2 条上游弃用提示。相比前一轮 74 项，本次新增 44 项回归，包含 31 项页面/模板/实际 Gradio 处理测试、12 项音频/下载/暂存测试及 1 项特殊启动路径测试。
- Python 编译、git diff --check 通过。测试工具仍隔离于 `audit/test-deps`，没有修改运行依赖。

### 实际页面状态链

隔离服务使用同一份新版界面和真实 Gradio 队列/会话。模型执行关闭，历史 B 使用前一轮真实短音频作为标明 `audit_fixture=true` 的固定样本；A 的阶段变化由临时 SQLite 测试记录控制，因此这部分不计为新的真实生成。

- 修复前：开始 A → 查看 B → A 结束轮询，页面仍显示 A queued、音频为空，但取消返回 succeeded，证实后台/页面不一致。
- 修复后：开始 A → 查看 B，页面显示“生成完成”并出现播放/下载；把 A 阶段推进到 encoding，页面仍保持 B。
- 点击“取消正在查看的任务”返回 succeeded；数据库 A 仍为 encoding，确认不会误取消 A。
- 清空文本再提交，页面提示“主要内容不能为空”，B 音频仍保留，取消目标仍为 B。
- 页面错误控制台为空；测试服务及测试页面已关闭，保留正式 7860 页面。

### 30 秒上限真实推理

- 独立测试数据库/输出，同一模型、运行环境与最新源码；Flash、CPU Offload=true、keep_loaded=false、seed=20260914。
- 任务 ID：`ea628378-a14d-4300-82f3-ed46200ddfdf`，状态 succeeded。
- 输出：**720000 帧，24000 Hz，30.0 秒**；全部样本有限，峰值 0.821346879。
- 参数记录耗时：39.802 秒；峰值已分配显存：17591997440 字节。
- 测试结束后 GPU 总占用约 974 MiB、利用率 0%；推理测试进程正常退出。
- 音频：`audit/third-long-task/outputs/ea628378-a14d-4300-82f3-ed46200ddfdf/result.wav`；完整证据：`audit/third-long-task/evidence.json`。
- 本项验证时长上限和技术链路，不代替逐类主观音质验收。

## 生效情况与仍未完成项

正式服务本轮核实仍为 PID 163968，启动时间 00:32:51；其 `/config` 中 Seed 仍是旧 number 组件，当前源码已改为 textbox，确认旧进程没有加载前两轮及本轮全部界面修复。

上一轮自动审批拒绝停止并重启正式服务，未给出具体原因。本轮没有绕过该限制；修复写入同一整合包后仍需重启 AuK 才会在正式页面生效。第二轮已同步的 ComfyUI 节点仍需重启 ComfyUI 并重新打开修正后的示例工作流。

仍待验收：16 类任务的固定素材主观音质与编辑/分离效果；另一台干净 Windows 机器的非管理员、离线和完整包迁移；多分辨率/系统缩放、录音权限和完整键盘操作；真实磁盘写满及整包版本回滚。临时目录、故障注入和模板字段检查不等同于这些完整验收。
