# AuK Local 五轮检查报告

检查日期：2026-09-14。范围：现有唯一整合包、本机服务、中文界面、已安装 ComfyUI 桥接节点。采用代码检查、临时目录故障注入、真实 Windows 子进程测试、浏览器操作及真实 GPU 推理；主 Agent 与子 Agent 交叉复核。

## 五轮结果

| 轮次 | 范围 | 发现、修复与验证 |
|---|---|---|
| 1 | 输入、模型输出与保存失败 | 修复坏上传音频绕过提交错误提示、空/NaN/Inf 输出被当作成功、参数文件写入失败留下 WAV、加载阶段未进入统一清理。先复现失败，再运行修复后回归。 |
| 2 | 队列、取消、进程生命周期 | 修复数据库短暂锁定导致调度退出、领取任务到启动间取消失效、终态落盘异常、取消期间的半文件、父服务强停后 worker 残留。12 项生命周期测试通过，包含真实 Windows spawn 父进程退出实验。 |
| 3 | 启动与诊断脚本 | 修复诊断入口丢失 --verify-hashes 参数、浏览器助手误打开其他服务、令牌尾随换行影响身份判断。模拟服务验证启动器与助手；诊断参数用临时脚本实际验证转发。 |
| 4 | 真实模型、界面和 ComfyUI | 三套模型完整 SHA-256 校验通过；已安装节点真实生成 1 秒 24 kHz 音频；页面实测排队取消和空文本错误提示，控制台无脚本错误。 |
| 5 | 交叉复核与最终回归 | 修复新 degraded 状态导致同包页面无法重开、成功结果被暂停提示遮蔽、Comfy 在调度暂停时无限等待及取消失败掩盖原始错误。最终 52 项自动测试、5 个启动场景、6 个 Comfy 模拟场景通过；静态检查和编译通过。 |

## 真实推理证据

- 任务 ID：`c005dcf5-c810-4004-80e0-8b24c96633bc`。
- 模型：AuK-Flash；seed：20260913；CPU Offload 开启。
- Comfy 输出形状：`[1, 1, 24000]`，采样率 24000 Hz，时长 1 秒。
- 全部样本有限，峰值约 0.077338；冷启动与推理共 59.537 秒。
- 文件：`outputs/c005dcf5-c810-4004-80e0-8b24c96633bc/result.wav` 与同目录 `metadata.json`。
- 此样例验证技术链路，不代表全部任务的主观音质验收。
- 最终服务重新启动后为 `status=ok`、`scheduler.state=ready`、`dispatcher_alive=true`、`inference_ready=true`。
- 已对真正的旧服务执行停止，确认新 watchdog 让其 worker 自动退出；先前 4 个确认父进程不存在的旧 worker 已清理。
- 最终节点源码与已安装节点 SHA-256 均为 `cc88bc1577fc292f6ac113fe349874ef04a404a52faa1422bed6990356a2a039`。

## 故障时的行为

- 短暂数据库错误有限重试；持续失败会暂停接收新任务并返回明确的 503/调度状态。
- 已生成结果仅重试保存任务状态，不重复跑 GPU；无法确认落盘时保留结果文件并显示待处理任务 ID。
- 存储恢复后需重新启动服务；历史中的中断/失败任务可重试。已成功任务不因其他任务失败而隐藏。
- 取消只清理该任务固定的结果和参数文件；成功提交的文件不会被取消竞争误删。
- 磁盘满测试采用 ENOSPC 和数据库写入故障注入，没有真的写满用户磁盘。

## 仍未完成的验收

1. 16 类任务的固定素材逐项听测，尤其编辑后的内容、声线、歌词/旋律保留与分离质量。
2. 另一台干净 Windows 电脑：非管理员、无系统 Python/Node/Git、中文与空格目录、离线运行。
3. 1366×768、1920×1080、125%/150% 系统缩放的完整界面覆盖，以及录音权限/完整键盘流程。
4. 真实磁盘写满和手工更新回滚演练。已有故障注入结果不能替代这些环境实测。

在本轮覆盖范围内，已复现缺陷均已修复并验证；上述待验收项目仍保留，不能据此保证所有电脑、素材和故障组合均无问题。
